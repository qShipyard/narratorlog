package main

import (
	"fmt"
	"os"
)

func main() {
	// TODO: initialize cobra/commander
	// TODO: register commands (init, generate, preview, history, status, create-plugin)

	fmt.Println("narratorlog CLI")
	fmt.Println("Run 'narratorlog --help' for usage.")
	os.Exit(0)
}
